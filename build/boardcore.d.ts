export declare function canMove(mat: number[][], i: number, j: number, ki: number): boolean;
export declare function showKi(array: number[][], steps: string[], isPonnuki?: boolean): {
    arrangement: number[][];
    hasMoved: boolean;
};
